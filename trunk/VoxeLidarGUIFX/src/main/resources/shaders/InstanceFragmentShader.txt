#version 140

flat in vec4 Color;
out vec4 outColor;
 
void main()
{
    if(Color.a == 0){
        discard;
    }
    if(Color.r == 0){
        discard;
    }
    outColor = vec4(Color.r, Color.g, Color.b, Color.r);
}